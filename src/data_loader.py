"""
Data Loader Module for Buy or Wait?
Handles loading, joining, and indexing CSV dataset files.
"""
from pathlib import Path
import pandas as pd

def load_all_data(dataset_dir: Path):
    """Load all CSV files from dataset directory."""
    pass
